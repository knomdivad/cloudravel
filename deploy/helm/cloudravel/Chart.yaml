apiVersion: v2
name: cloudravel
description: CloudRavel — multi-cloud AIOps operating platform (API + web + bundled SQL/storage/secret-store)
type: application
# Chart version — bump on chart changes. appVersion tracks the app image tag default.
version: 0.1.0
appVersion: "1.0.0"
kubeVersion: ">=1.23.0-0"
home: https://github.com/knomdivad/azureinventorymanager
sources:
  - https://github.com/knomdivad/azureinventorymanager
maintainers:
  - name: CloudRavel
